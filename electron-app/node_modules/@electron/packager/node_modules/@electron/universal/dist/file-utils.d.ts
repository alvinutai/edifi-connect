export declare const MACHO_MAGIC: Set<number>;
export declare const MACHO_UNIVERSAL_MAGIC: Set<number>;
export declare const isMachO: (header: Buffer) => boolean;
/**
 * Glob match with matchBase semantics: if the pattern contains no `/`,
 * only the basename of `filePath` is tested against the pattern.
 */
export declare const matchGlob: (filePath: string, pattern: string) => boolean;
export type DiffEntry = {
    state: 'equal' | 'distinct' | 'left' | 'right';
    name1?: string;
    relativePath: string;
};
export declare function compareDirectories(dir1: string, dir2: string): Promise<DiffEntry[]>;
export declare enum AppFileType {
    MACHO = 0,
    PLAIN = 1,
    INFO_PLIST = 2,
    SNAPSHOT = 3,
    APP_CODE = 4,
    SINGLE_ARCH = 5
}
export type AppFile = {
    relativePath: string;
    type: AppFileType;
};
export type GetAllAppFilesOpts = {
    singleArchFiles?: string;
};
/**
 *
 * @param appPath Path to the application
 */
export declare const getAllAppFiles: (appPath: string, opts: GetAllAppFilesOpts) => Promise<AppFile[]>;
export declare const readMachOHeader: (path: string) => Promise<Buffer<ArrayBuffer>>;
export declare const fsMove: (oldPath: string, newPath: string) => Promise<void>;
